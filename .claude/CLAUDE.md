Do NOT commit without the user asking you to commit. 

When asked to commit, use `gencommit` to commit. It replaces git commit -m "<message>" with a generated message. So `git add <files>` then `gencommit`.
